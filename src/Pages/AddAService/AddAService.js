import React from 'react';
import { Helmet } from 'react-helmet';

const AddAService = () => {
    return (
        <div>
            <Helmet>
                <title>Add a service | Eye Specialist</title>
            </Helmet>
        </div>
    );
};

export default AddAService;